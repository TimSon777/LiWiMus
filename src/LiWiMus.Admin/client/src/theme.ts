import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#ed6c02",
    },
    background: {
      default: "#727271",
      paper: "#444444",
    },
  },
});

export default theme;
